import 'package:flutter/material.dart';

const Color containerColor = Color(0xFFF3F3F3);
const Color textContainerColor = Color(0xFFD9D9D9);
const Color textcolor = Color(0xFF082585);

const String quickGuide = '''
Tap on the images of the candidate to select them
in priority. Priority is based upon the order of selection. The first selection gets the highest priority while the last selection gets the lowest.
Tap on RESET button if you want to reset your selection
''';
